# 🚀 XHS-MarketAI 快速运行指南

## 系统简介

**自进化数据挖掘机 (Self-Evolving Data Miner)**  
一个能够自主深度挖掘、自我纠错、自动停止的智能市场洞察系统。

### 核心能力
- 🤖 **自主推演**：AI 自动生成下一轮分析指令
- 🛡️ **防幻觉**：硬核数据验证 + 逻辑审计
- 🔄 **自我纠错**：发现错误自动反馈修正
- 🎯 **智能停止**：向量相似度 + 关键词密度双重判定

---

## 方法一：自动化安装（推荐）

### Windows 用户

1. **双击运行** `setup.bat`
2. 等待安装完成
3. 设置 API Key：
   ```powershell
   $env:DEEPSEEK_API_KEY="sk-your-api-key-here"
   ```
4. 运行系统：
   ```powershell
   python market_insight_system.py
   ```

### macOS/Linux 用户

1. 运行安装脚本：
   ```bash
   chmod +x setup.sh
   ./setup.sh
   ```
2. 激活环境：
   ```bash
   source venv/bin/activate
   ```
3. 设置 API Key：
   ```bash
   export DEEPSEEK_API_KEY="sk-your-api-key-here"
   ```
4. 运行系统：
   ```bash
   python market_insight_system.py
   ```

---

## 方法二：手动安装

### 1. 创建虚拟环境

```bash
# Windows
python -m venv venv
.\venv\Scripts\Activate.ps1

# macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 2. 安装依赖

```bash
pip install pandas openpyxl openai numpy
```

或使用 requirements.txt:

```bash
pip install -r requirements.txt
```

### 3. 配置 API Key

获取 DeepSeek API Key: https://platform.deepseek.com/

**Windows PowerShell:**
```powershell
$env:DEEPSEEK_API_KEY="sk-your-api-key-here"
```

**macOS/Linux:**
```bash
export DEEPSEEK_API_KEY="sk-your-api-key-here"
```

### 4. 运行系统

```bash
python market_insight_system.py
```

**运行时选择模式**：
- 输入 `1`：单轮测试（只运行第一轮，快速验证）
- 输入 `2`：完整循环（自动迭代直到停止，完整挖掘）

---

## 方法三：测试模式（无需 API Key）

如果你想先测试系统架构，无需真实 API：

```bash
python test_system.py
```

这将使用模拟数据和预设响应运行系统，验证所有组件正常工作。

---

## 📦 依赖包说明

| 包名 | 版本 | 用途 |
|------|------|------|
| pandas | ≥2.0.0 | 数据处理和分析 |
| openpyxl | ≥3.1.0 | Excel 文件读取 |
| openai | ≥1.0.0 | DeepSeek API 客户端 |
| numpy | ≥1.24.0 | 向量计算（余弦相似度） |

---

## 🎯 运行模式说明

### 模式 1: 单轮测试
- **适用场景**：首次运行、快速验证、调试
- **执行内容**：只运行第一轮分析
- **输出文件**：`insight_result_round1.json`
- **耗时**：约 10-30 秒

### 模式 2: 完整循环（推荐）
- **适用场景**：正式分析、深度挖掘
- **执行内容**：自动迭代直到系统判定停止
- **停止条件**：
  - 连续2轮相似度 > 0.9
  - 达到最大轮数（5轮）
  - 审计未通过
  - 新关键词率 < 20% 且相似度高
- **输出文件**：`insight_full_cycle.json`
- **耗时**：约 1-5 分钟（取决于迭代轮数）

---

## 📊 输出文件说明

### 单轮输出

**JSON 格式**（机器可读）：`insight_result_round1.json`
```json
{
  "status": "Success",
  "iteration": 1,
  "audit_report": {...},
  "analyst_result": {
    "insight": "洞察结论...",
    "evidence_trace": [...]
  },
  ...
}
```

**Markdown 格式**（人类友好）：`insight_result_round1.md`
- 包含核心洞察、数据证据表格、审计结果、下一步建议
- 可直接在 VS Code 中预览（Ctrl+Shift+V）
- 适合阅读、分享和汇报

### 完整循环输出

**JSON 格式**：`insight_full_cycle.json`
```json
{
  "total_iterations": 3,
  "total_insights": 3,
  "total_keywords": 45,
  "all_rounds": [
    { /* 第1轮完整结果 */ },
    { /* 第2轮完整结果 */ },
    { /* 第3轮完整结果 */ }
  ],
  "final_insight_summary": [...]
}
```

**Markdown 格式**：`insight_full_cycle.md`
- 完整的分析报告，包含：
  - 📊 执行摘要
  - 🎯 分析目标
  - 💡 核心发现汇总
  - 📋 详细分析过程（每轮的洞察、证据表格、审计结果）
  - 🛑 停止原因
  - 🔑 关键词发现

### 测试模式输出

**文件**：`test_result.json` + `test_result.md`

---

## 🔧 常见问题

### Q1: PowerShell 提示权限错误

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q2: pip 安装速度慢

使用清华镜像源：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q3: API Key 在哪里获取？

访问 DeepSeek 官网：https://platform.deepseek.com/
1. 注册账号
2. 进入 API Keys 页面
3. 创建新的 API Key
4. 复制密钥（形如 `sk-xxxxxxxx`）

### Q4: 数据文件在哪里？

确保以下文件在项目目录中：
- `搜索词-2025-6月.xlsx - Sheets1.csv`
- `搜索词-2025-12月.xlsx - Sheets1.csv`

如果文件名不同，修改 `market_insight_system.py` 中的 `file_paths` 参数。

### Q5: 如何查看详细运行日志？

系统会在控制台实时输出每一步的执行情况：
- **Analyst** 分析进度
- **Auditor** 验证详情（每条证据的验证结果）
- **Controller** 决策信息（新鲜度检查、相似度、新关键词率）

### Q6: 系统为什么提前停止了？

可能原因：
1. **审计未通过**：AI 引用了不存在的数据
2. **连续高相似度**：连续2轮洞察相似度 > 0.9
3. **信息密度低**：新关键词率 < 20%
4. **达到最大轮数**：默认 5 轮

检查控制台输出的停止原因即可。

### Q7: 如何自定义最大迭代次数？

在 `market_insight_system.py` 中修改：
```python
system = MarketInsightSystem(api_key=api_key)
system.max_iterations = 10  # 改为 10 轮
```

---

## 🎬 运行示例

### 完整运行日志示例

```
================================================================================
XHS-MarketAI System - 自进化数据挖掘机
================================================================================

✓ 加载数据: 6月 - 150 行, 8 列
✓ 加载数据: 12月 - 150 行, 8 列

请选择运行模式:
1. 单轮测试（只运行第一轮）
2. 完整循环（自动迭代直到停止）

请输入选择 (1/2，默认2): 2

================================================================================
🚀 XHS-MarketAI 洞察闭环系统 - 自进化数据挖掘机
================================================================================

============================================================
第 1 轮挖掘
============================================================

[Analyst] 正在分析数据...
洞察: 桃子水在6月搜索指数为56516，12月降至23456，下降58.5%...

[Auditor] 正在验证证据...
============================================================
审计结果: 2/2 条证据通过验证
============================================================
证据 1: ✅ 验证通过
  - 关键词: '桃子水'
  - 数据源: 6月
  - 搜索指数: 56516 (声称值: 56516)
  - 行号: 5

证据 2: ✅ 验证通过
  - 关键词: '桃子水'
  - 数据源: 12月
  - 搜索指数: 23456 (声称值: 23456)
  - 行号: 3

【逻辑审计】
✅ 逻辑审计通过

[Controller - 新鲜度检查] Novelty Checker 启动...
  📊 向量余弦相似度: 0.000 (阈值: 0.90)
  🔑 新关键词数: 12/15 (80.0%)
  📝 新关键词: 桃子水, 爽身粉, 季节性, 高温, 护理...
  ✅ 新鲜度检查通过 (相似度: 0.00%, 新词率: 80.0%)

[Controller] 决策: CONTINUE
[Controller] 原因: 审计通过且存在新发现，继续深度挖掘

============================================================
第 2 轮挖掘
============================================================
...

================================================================================
🛑 系统停止：连续 2 轮高相似度 (>90.5%)，数据价值已被榨干
================================================================================

================================================================================
📊 挖掘完成总结
================================================================================
总轮数: 3
有效洞察数: 3
证据总数: 8
发现关键词数: 45

✅ 完整结果已保存至: insight_full_cycle.json

================================================================================
🔍 关键发现汇总
================================================================================

第 1 轮: 桃子水在6月搜索指数为56516，12月降至23456，下降58.5%...
第 2 轮: 淡季布局机会：婴儿湿疹相关词在12月仍保持稳定搜索...
第 3 轮: 松达品牌在爽身粉类目中...
```

---

## 📚 深入了解

- 📖 [完整系统架构说明](SYSTEM_ARCHITECTURE.md)
- ✅ [需求对照清单](REQUIREMENTS_CHECKLIST.md)
- 📘 [详细使用文档](README.md)

---

## 💡 使用建议

### 首次使用：
1. 先运行 `test_system.py` 验证环境
2. 使用模式 1（单轮测试）快速验证
3. 确认无误后使用模式 2（完整循环）

### 正式分析：
1. 准备好完整数据文件
2. 编写清晰的初始战略目标
3. 使用模式 2 进行深度挖掘
4. 查看 `insight_full_cycle.json` 获取完整结果

### 调试优化：
1. 观察控制台输出的新鲜度检查信息
2. 调整 `max_iterations` 控制迭代次数
3. 修改 `user_initial_prompt` 优化分析角度

---

**准备就绪？开始运行吧！** 🚀

```bash
python market_insight_system.py
```


## 方法一：自动化安装（推荐）

### Windows 用户

1. **双击运行** `setup.bat`
2. 等待安装完成
3. 设置 API Key：
   ```powershell
   $env:DEEPSEEK_API_KEY="sk-your-api-key-here"
   ```
4. 运行系统：
   ```powershell
   python market_insight_system.py
   ```

### macOS/Linux 用户

1. 运行安装脚本：
   ```bash
   chmod +x setup.sh
   ./setup.sh
   ```
2. 激活环境：
   ```bash
   source venv/bin/activate
   ```
3. 设置 API Key：
   ```bash
   export DEEPSEEK_API_KEY="sk-your-api-key-here"
   ```
4. 运行系统：
   ```bash
   python market_insight_system.py
   ```

---

## 方法二：手动安装

### 1. 创建虚拟环境

```bash
# Windows
python -m venv venv
.\venv\Scripts\Activate.ps1

# macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 2. 安装依赖

```bash
pip install pandas openpyxl openai
```

或使用 requirements.txt:

```bash
pip install -r requirements.txt
```

### 3. 配置 API Key

获取 DeepSeek API Key: https://platform.deepseek.com/

**Windows PowerShell:**
```powershell
$env:DEEPSEEK_API_KEY="sk-your-api-key-here"
```

**macOS/Linux:**
```bash
export DEEPSEEK_API_KEY="sk-your-api-key-here"
```

### 4. 运行系统

```bash
python market_insight_system.py
```

---

## 方法三：测试模式（无需 API Key）

如果你想先测试系统架构，无需真实 API：

```bash
python test_system.py
```

这将使用模拟数据和预设响应运行系统，验证所有组件正常工作。

---

## 📦 依赖包说明

| 包名 | 版本 | 用途 |
|------|------|------|
| pandas | ≥2.0.0 | 数据处理和分析 |
| openpyxl | ≥3.1.0 | Excel 文件读取 |
| openai | ≥1.0.0 | DeepSeek API 客户端 |

---

## 🔧 常见问题

### Q1: PowerShell 提示权限错误

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q2: pip 安装速度慢

使用清华镜像源：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q3: API Key 在哪里获取？

访问 DeepSeek 官网：https://platform.deepseek.com/
1. 注册账号
2. 进入 API Keys 页面
3. 创建新的 API Key
4. 复制密钥（形如 `sk-xxxxxxxx`）

### Q4: 数据文件在哪里？

确保以下文件在项目目录中：
- `搜索词-2025-6月.xlsx - Sheets1.csv`
- `搜索词-2025-12月.xlsx - Sheets1.csv`

如果文件名不同，修改 `market_insight_system.py` 中的 `file_paths` 参数。

### Q5: 如何查看详细日志？

系统会在控制台实时输出每一步的执行情况，包括：
- 数据加载状态
- Analyst 分析结果
- Auditor 验证日志
- Controller 决策信息

最终结果保存在 `insight_result_round1.json`。

---

## 📊 预期输出示例

```json
{
  "status": "Success",
  "iteration": 1,
  "audit_report": {
    "is_passed": true,
    "log": "证据 1: ✓ '桃子水' 在 6月 中 搜索指数=56516 (声称值=56516)\n证据 2: ✓ '桃子水' 在 12月 中 搜索指数=23456 (声称值=23456)"
  },
  "analyst_result": {
    "insight": "桃子水在6月搜索指数显著高于12月...",
    "evidence_trace": [
      {"date": "6月", "keyword": "桃子水", "search_index": 56516, "row": 5},
      {"date": "12月", "keyword": "桃子水", "search_index": 23456, "row": 3}
    ]
  },
  "system_generated_next_prompt": "深入分析：在12月淡季，哪些相关长尾词...",
  "controller_decision": "CONTINUE"
}
```

---

## 🎯 下一步

系统成功运行后，你可以：

1. **查看生成的 JSON 文件**，了解第一轮洞察结果
2. **修改 `user_initial_prompt`**，尝试不同的分析角度
3. **实现多轮迭代**，让系统自动深挖（参考 README.md）
4. **扩展系统功能**，添加自定义分析逻辑

---

## 💡 提示

- 首次运行建议使用 `test_system.py` 验证环境配置
- API 调用会消耗费用，注意控制迭代次数
- 如遇到问题，检查 Python 版本（需 ≥3.8）

**准备就绪？开始运行吧！** 🚀
